#+TITLE: Bundle Packaging — Task Tracker
#+STARTUP: overview logdone
#+TODO: TODO(t) IN-PROGRESS(i!) BLOCKED(b@/!) | DONE(d!) WONTFIX(w@/!)
#+PROPERTY: header-args :eval no
#+FILETAGS: :workstream:bundle-packaging:

* Overview
This tracker drives the work described in [[file:design.md][design.md]].

* Decisions
** Archive format
   :PROPERTIES:
   :DECIDED_BY:
   :DECIDED_ON:
   :END:
   - Choice:
   - Rationale:

** Manifest schema
   :PROPERTIES:
   :DECIDED_BY:
   :DECIDED_ON:
   :END:
   - Choice:
   - Rationale:

** Include and exclude set
   :PROPERTIES:
   :DECIDED_BY:
   :DECIDED_ON:
   :END:
   - Choice:
   - Rationale:

** Overwrite policy
   :PROPERTIES:
   :DECIDED_BY:
   :DECIDED_ON:
   :END:
   - Choice: unpack refuses non-empty destination unless =--force=.
   - Rationale:

* Phase 0 — Preflight :phase0:
** TODO P0.1 Confirm single-command dependency landed
   :PROPERTIES:
   :ID:       P0.1
   :OWNER:
   :STARTED:
   :FINISHED:
   :DEPENDS:
   :END:
   DoD: =draccus bundle show --help= exists and Gate 0 baseline is captured.

** TODO P0.2 Fill decisions
   :PROPERTIES:
   :ID:       P0.2
   :OWNER:
   :STARTED:
   :FINISHED:
   :DEPENDS:  P0.1
   :END:
   DoD: all decisions filled.

* Phase 1 — Manifest and show :phase1:
** TODO P1.1 Implement bundle manifest generation
   :PROPERTIES:
   :ID:       P1.1
   :OWNER:
   :STARTED:
   :FINISHED:
   :DEPENDS:  P0.2
   :END:
   DoD: manifest captures paths, included components, created timestamp, schema version, and bundle identity.

** TODO P1.2 Implement =draccus bundle show=
   :PROPERTIES:
   :ID:       P1.2
   :OWNER:
   :STARTED:
   :FINISHED:
   :DEPENDS:  P1.1
   :END:
   DoD: text and JSON output work for current/default bundle; Gate 0 passes.

* Phase 2 — Pack :phase2:
** TODO P2.1 Implement =draccus bundle pack=
   :PROPERTIES:
   :ID:       P2.1
   :OWNER:
   :STARTED:
   :FINISHED:
   :DEPENDS:  P1.1
   :END:
   DoD: current bundle archives with expected include/exclude set and size report; Gate 0 passes.

* Phase 3 — Unpack :phase3:
** TODO P3.1 Implement =draccus bundle unpack=
   :PROPERTIES:
   :ID:       P3.1
   :OWNER:
   :STARTED:
   :FINISHED:
   :DEPENDS:  P2.1
   :END:
   DoD: archive installs to default destination, refuses overwrite without =--force=, and unpacked bundle passes doctor.

* Phase 4 — Docs and acceptance :phase4:
** TODO P4.1 Document local distribution workflow
   :PROPERTIES:
   :ID:       P4.1
   :OWNER:
   :STARTED:
   :FINISHED:
   :DEPENDS:  P3.1
   :END:
   DoD: README/DESIGN/roadmap describe pack/unpack/show and default layout.

** TODO P4.2 Acceptance and retrospective
   :PROPERTIES:
   :ID:       P4.2
   :OWNER:
   :STARTED:
   :FINISHED:
   :DEPENDS:  P4.1
   :END:
   DoD: pack/unpack/show exercised, Gate 0 passes, retrospective filled.

* Retrospective
- Surprises:
- Automate next time:
- Doc gaps:

* Notes
