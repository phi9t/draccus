#+TITLE: Runtime Provenance — Task Tracker
#+STARTUP: overview logdone
#+TODO: TODO(t) IN-PROGRESS(i!) BLOCKED(b@/!) | DONE(d!) WONTFIX(w@/!)
#+PROPERTY: header-args :eval no
#+FILETAGS: :workstream:runtime-provenance:

* Overview
This tracker drives the work described in [[file:design.md][design.md]].

* Decisions
** Git capture and redaction
   :PROPERTIES:
   :DECIDED_BY:
   :DECIDED_ON:
   :END:
   - Choice:
   - Rationale:

** Environment capture allowlist
   :PROPERTIES:
   :DECIDED_BY:
   :DECIDED_ON:
   :END:
   - Choice:
   - Rationale:

** Import provenance package set
   :PROPERTIES:
   :DECIDED_BY:
   :DECIDED_ON:
   :END:
   - Choice:
   - Rationale:

* Phase 0 — Preflight :phase0:
** TODO P0.1 Confirm recorded run baseline
   :PROPERTIES:
   :ID:       P0.1
   :OWNER:
   :STARTED:
   :FINISHED:
   :DEPENDS:
   :END:
   DoD: current recorded run layout captured under artifacts.

** TODO P0.2 Fill decisions
   :PROPERTIES:
   :ID:       P0.2
   :OWNER:
   :STARTED:
   :FINISHED:
   :DEPENDS:  P0.1
   :END:
   DoD: all provenance decisions filled.

* Phase 1 — Schema :phase1:
** TODO P1.1 Define run metadata schema
   :PROPERTIES:
   :ID:       P1.1
   :OWNER:
   :STARTED:
   :FINISHED:
   :DEPENDS:  P0.2
   :END:
   DoD: schema is documented, versioned, and represented in sample artifacts.

* Phase 2 — Project and git metadata :phase2:
** TODO P2.1 Capture project config and git status
   :PROPERTIES:
   :ID:       P2.1
   :OWNER:
   :STARTED:
   :FINISHED:
   :DEPENDS:  P1.1
   :END:
   DoD: run records include bounded project config snapshot and git summary.

* Phase 3 — Import provenance :phase3:
** TODO P3.1 Capture foundation package provenance
   :PROPERTIES:
   :ID:       P3.1
   :OWNER:
   :STARTED:
   :FINISHED:
   :DEPENDS:  P1.1
   :END:
   DoD: torch/jax/jaxlib/numpy/scipy/triton/nvidia provenance is captured and checked against the foundation path.

* Phase 4 — Docs and acceptance :phase4:
** TODO P4.1 Document provenance contract
   :PROPERTIES:
   :ID:       P4.1
   :OWNER:
   :STARTED:
   :FINISHED:
   :DEPENDS:  P2.1 P3.1
   :END:
   DoD: docs explain fields, redaction, and known limits.

** TODO P4.2 Acceptance and retrospective
   :PROPERTIES:
   :ID:       P4.2
   :OWNER:
   :STARTED:
   :FINISHED:
   :DEPENDS:  P4.1
   :END:
   DoD: sample runs validate metadata; Gate 0 passes; retrospective filled.

* Retrospective
- Surprises:
- Automate next time:
- Doc gaps:

* Notes
